{
  "$schema": "https://railway.app/railway.schema.json",

  "//": "Build pipeline lives in the root Dockerfile (Nixpacks kept ignoring nixpacks.toml). This file controls deploy + healthcheck.",

  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/api/health",
    "//healthcheckTimeout": "90s gives the bootstrap room to hydrate from Postgres (4 hydration awaits + the canonical contractor seed migration). The api-server itself answers /api/health in <5ms once listening.",
    "healthcheckTimeout": 90,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
